import 'package:flutter/material.dart';

/// Paleta de colores para la app de Auditoría Financiera
class AppColors {
  // Fondos y Superficies
  static const Color background = Color(0xFF0F172A); // Slate 900
  static const Color surface = Color(0xFF1E293B);    // Slate 800
  static const Color surfaceLight = Color(0xFF334155); // Slate 700
  static const Color surfaceCard = Color(0xFF1E293B);

  // Colores de Acento y Marca
  static const Color primary = Color(0xFF10B981);       // Emerald 500 (Ahorro/Finanzas)
  static const Color primaryDark = Color(0xFF059669);   // Emerald 600
  static const Color primaryLight = Color(0xFF34D399);  // Emerald 400
  static const Color secondary = Color(0xFF6366F1);     // Indigo 500
  static const Color accent = Color(0xFF38BDF8);        // Sky 400

  // Semántica Financiera
  static const Color income = Color(0xFF10B981);        // Verde para Ingresos
  static const Color incomeBackground = Color(0xFF064E3B);
  static const Color expense = Color(0xFFEF4444);       // Rojo para Egresos/Gastos
  static const Color expenseBackground = Color(0xFF7F1D1D);
  static const Color warning = Color(0xFFF59E0B);       // Ámbar para Alertas

  // Textos
  static const Color textPrimary = Color(0xFFF8FAFC);   // Slate 50
  static const Color textSecondary = Color(0xFF94A3B8); // Slate 400
  static const Color textMuted = Color(0xFF64748B);     // Slate 500

  // Bordes y Divisores
  static const Color border = Color(0xFF334155);        // Slate 700
  static const Color borderSubtle = Color(0xFF1E293B);
}
